using System.Collections;
using UnityEngine;
using UnityEngine.UI;

public class Inibir_btn : MonoBehaviour {

	public GameObject aaa;

	void Start () {
		if (scoreNthings.intru == -1)
		aaa.SetActive(false);
	}
}
